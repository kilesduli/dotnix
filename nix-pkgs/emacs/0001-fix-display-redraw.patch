From c7d97daca774f5df169c692f6ab02d9199b6a514 Mon Sep 17 00:00:00 2001
From: duli <duli4868@gmail.com>
Date: Mon, 15 Jun 2026 11:44:40 +0800
Subject: [PATCH] fix-display-redraw

---
 src/dispextern.h |  2 ++
 src/dispnew.c    | 18 +++++++++++++++++-
 src/xdisp.c      | 18 ++++++++++++++++++
 src/xterm.c      |  7 +++++++
 src/xterm.h      |  3 +++
 5 files changed, 47 insertions(+), 1 deletion(-)

diff --git a/src/dispextern.h b/src/dispextern.h
index ef301ee45ac..bef8570a94b 100644
--- a/src/dispextern.h
+++ b/src/dispextern.h
@@ -3067,6 +3067,8 @@ reset_mouse_highlight (Mouse_HLInfo *hlinfo)
   int height, nrows;
 };
 
+bool redisplay_avoid_pixel_scroll_p (struct frame *);
+
 
 /* Handlers for setting frame parameters.  */
 
diff --git a/src/dispnew.c b/src/dispnew.c
index d7141f953cc..12deaaa5911 100644
--- a/src/dispnew.c
+++ b/src/dispnew.c
@@ -105,6 +105,20 @@ Copyright (C) 1985-1988, 1993-1995, 1997-2026 Free Software Foundation,
 static void tty_set_cursor (struct frame *f);
 
 
+bool
+redisplay_avoid_pixel_scroll_p (struct frame *f)
+{
+#if defined HAVE_X_WINDOWS && defined USE_LUCID && !defined USE_GTK
+  /* Lucid runs as an X11 client.  Under Xwayland, XCopyArea-based
+     redisplay scrolling can copy stale pixels back into the frame, so
+     prefer repainting the affected rows.  */
+  return FRAME_X_P (f) && FRAME_DISPLAY_INFO (f)->xwayland_p;
+#else
+  return false;
+#endif
+}
+
+
 #if 0 /* Please leave this in as a debugging aid.  */
 static void
 check_rows (struct frame *f)
@@ -4537,7 +4551,9 @@ update_window (struct window *w)
     ++row;
 
   /* Try reusing part of the display by copying.  */
-  if (row < end && !desired_matrix->no_scrolling_p)
+  if (row < end
+      && !desired_matrix->no_scrolling_p
+      && !redisplay_avoid_pixel_scroll_p (XFRAME (WINDOW_FRAME (w))))
     {
       int rc = scrolling_window (w, (tab_line_row != NULL ? 1 : 0)
 				    + (header_line_row != NULL ? 1 : 0));
diff --git a/src/xdisp.c b/src/xdisp.c
index 9647104ada8..11e15f1e231 100644
--- a/src/xdisp.c
+++ b/src/xdisp.c
@@ -21939,6 +21939,12 @@ try_window_reusing_current_matrix (struct window *w)
 
 	  if (run.height > 0 && run.current_y != run.desired_y)
 	    {
+	      if (redisplay_avoid_pixel_scroll_p (f))
+		{
+		  clear_glyph_matrix (w->desired_matrix);
+		  return false;
+		}
+
 #ifdef HAVE_WINDOW_SYSTEM
 	      update_begin (f);
 	      gui_update_window_begin (w);
@@ -22107,6 +22113,12 @@ try_window_reusing_current_matrix (struct window *w)
 
       if (run.height)
 	{
+	  if (redisplay_avoid_pixel_scroll_p (f))
+	    {
+	      clear_glyph_matrix (w->desired_matrix);
+	      return false;
+	    }
+
 #ifdef HAVE_WINDOW_SYSTEM
 	  update_begin (f);
 	  gui_update_window_begin (w);
@@ -23117,6 +23129,12 @@ #define GIVE_UP(X) return 0
      found.  */
   if (dy && run.height)
     {
+      if (FRAME_WINDOW_P (f) && redisplay_avoid_pixel_scroll_p (f))
+	{
+	  clear_glyph_matrix (w->desired_matrix);
+	  return 0;
+	}
+
       update_begin (f);
 
       if (FRAME_WINDOW_P (f))
diff --git a/src/xterm.c b/src/xterm.c
index e9b551502c2..dcaf0ba21ab 100644
--- a/src/xterm.c
+++ b/src/xterm.c
@@ -31228,6 +31228,13 @@ #define NUM_ARGV 10
     = XineramaQueryExtension (dpy, &xin_event_base, &xin_error_base);
 #endif
 
+  {
+    int opcode, event_base, error_base;
+    dpyinfo->xwayland_p
+      = XQueryExtension (dpyinfo->display, "XWAYLAND",
+			 &opcode, &event_base, &error_base);
+  }
+
   /* See if a private colormap is requested.  */
   if (dpyinfo->visual == DefaultVisualOfScreen (dpyinfo->screen))
     {
diff --git a/src/xterm.h b/src/xterm.h
index 20633f283bd..09df33425c1 100644
--- a/src/xterm.h
+++ b/src/xterm.h
@@ -847,6 +847,9 @@ #define X_INVALID_WINDOW 0xffffffff
   bool supports_xdbe;
 #endif
 
+  /* True if this X server is Xwayland.  */
+  bool xwayland_p;
+
 #ifdef HAVE_XINPUT2
   bool supports_xi2;
 
-- 
2.54.0

